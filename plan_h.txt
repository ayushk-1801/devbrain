"""
Plan 1 - Issues Ingestion Implementation

Summary:
==========
Add GitHub Issues ingestion to DevBrain. Track closed issues with their
full discussion comments, complementing the existing PR tracking system.

Files Modified/Created:
========================
1. backend/ingestion/issues.py (NEW)
   - _issue_payload() - formats issue data with comments
   - ingest_issue() / ingest_issues() - single and batch ingestion

2. backend/ingestion/github_client.py
   - Added fetch_issues(), fetch_issue(), _issue_to_dict()

3. backend/ingestion/__init__.py - Export issues module

4. backend/main.py
   - Added _handle_issue() webhook handler for closed issues

5. backend/service.py
   - Added issues to full_sync() + ingest_single_issue()

6. backend/mcp_server.py
   - Added ingest_issues() MCP tool

Graph Schema Impact:
- Issue nodes (title, author, closed timestamp, state, labels)
- Comment nodes (author, body)
- Issue -> AUTHORED_BY -> Developer
- Comment -> REPLIES_TO -> Issue


Plan 2 - PR Discussion Comments Addition

Summary:
==========
Add general PR discussion comments (issue comments on PRs) alongside
existing review comments. PRs now capture both line-by-line code review
feedback AND general conversation.

Files Modified:
================
1. backend/ingestion/github_client.py
   - Added discussion_comments field via pr.get_issue_comments()
   - Added _extract_reviews() for full review summaries

2. backend/ingestion/pull_requests.py
   - Updated _pr_payload() to include discussion comments section


Plan 3 - Deep AST, Releases/Tags, PR Review Summaries

Summary:
==========
Three enhancements to complete the ingestion layer:

A) Deeper AST via codegraph module (codegraph.py)
   - Richer code structure with module dependency inference
   - Language breakdown, file categorization
   - Fallback when cognee[codegraph] not installed

B) Releases / Tags / Deployments ingestion (releases.py)
   - GitHub releases with notes and assets
   - Git tags with commit metadata
   - Deployments with environment and status

C) PR Review Summaries (APPROVED/REQUEST_CHANGES bodies)
   - Full review state + body text captured
   - Enables "why was this PR approved/requested changes?" queries

Files Created:
===============
1. backend/ingestion/codegraph.py (NEW)
   - ingest_codegraph() - deep code structure ingestion
   - _extract_entities_from_tree() - module dependency inference

2. backend/ingestion/releases.py (NEW)
   - ingest_releases() / ingest_tags() / ingest_deployments()
   - ingest_all_releases() - combined ingestion

Files Modified:
===============
3. backend/ingestion/github_client.py
   - Added fetch_releases(), fetch_tags(), fetch_deployments()
   - Added _extract_reviews() for PR review summaries
   - Added reviews field to _pr_to_dict()

4. backend/ingestion/pull_requests.py
   - Added Reviews section to _pr_payload()

5. backend/ingestion/__init__.py
   - Added codegraph and releases exports

6. backend/service.py
   - Replaced ast_modules with codegraph in full_sync()
   - Added releases to full_sync()
   - Added enqueue_release(), enqueue_pr_review()

7. backend/main.py
   - Added _handle_release() for published release events
   - Added _handle_pull_request_review() for review events
   - Added release and pull_request_review to webhook dispatch

Graph Schema Impact:
- Release nodes (tag, name, notes, author, published date)
- Tag nodes (name, sha, commit message, author, date)
- Deployment nodes (id, ref, sha, environment, status)
- Review nodes (author, state, body, submitted_at) on PRs
- Release -> TAGGED_BY -> Commit
- Deployment -> DEPLOYED_FROM -> Commit

Dataset Naming:
- repo_{owner}_{repo}_releases (releases + tags + deployments)
- repo_{owner}_{repo}_codegraph (deep code structure)
- repo_{owner}_{repo}_prs (now includes reviews field)

Query Examples:
===============
"What release introduced the breaking change?"

"Which PRs were approved with requested changes before merging?"

"What was the deployment status for the v2.0 release?"

"Show all REQUEST_CHANGES reviews on the payments module refactor"

"Which modules depend on PaymentGateway based on code structure?"
"""
